# No API key is required to run the pipeline — it is fully local/deterministic by default.
# Optional: set this only if you want LLM-assisted reason-text polish or media captioning (Phase 4/7 stretch).
# ANTHROPIC_API_KEY=